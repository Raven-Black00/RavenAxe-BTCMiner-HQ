# RavenMiner HQ v3.9.1 - Python dependencies
# Install: pip install -r requirements.txt

requests>=2.28.0
Pillow>=9.0.0
pystray>=0.19.0

# Build-time only (not needed to run the .py directly)
# pyinstaller>=6.0.0
